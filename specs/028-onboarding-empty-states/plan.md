# Implementation Plan: Onboarding & Empty States

**Branch**: `028-onboarding-empty-states` | **Date**: 2026-02-14 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `/specs/028-onboarding-empty-states/spec.md`

## Summary

Add a 3-step 2000s-era welcome wizard for first-time users and contextual empty states for 4 UI containers. The wizard extends ConnectLive with localStorage-based first-run detection. Empty states use conditional rendering in existing components with shared CSS. No database migrations required.

## Technical Context

**Language/Version**: Elixir 1.17+ / OTP 27+, JavaScript ES2020+
**Primary Dependencies**: Phoenix 1.8+, Phoenix LiveView 1.0+, retro CSS framework, esbuild
**Storage**: localStorage (client-side onboarding flag) — no PostgreSQL changes
**Testing**: ExUnit (unit, integration, LiveView), Vitest + jsdom (JS lib/hook tests)
**Target Platform**: Web browser (desktop-first, retro aesthetic)
**Project Type**: Umbrella (retro_hex_chat + retro_hex_chat_web)
**Performance Goals**: Empty states disappear within same render cycle as content arrival
**Constraints**: Wizard must not re-trigger on subsequent visits; all empty state text non-selectable
**Scale/Scope**: 1 new component, 4 modified components, 2 new CSS files, 1 new JS hook + lib

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Relevant? | Status | Notes |
|-----------|-----------|--------|-------|
| I. Elixir & Phoenix Exclusive | Yes | PASS | No JS UI frameworks. LiveView for all UI. JS hook is minimal (localStorage only). |
| II. Umbrella Bounded Contexts | Yes | PASS | No domain layer changes. All changes in retro_hex_chat_web (presentation). |
| III. OTP Process Architecture | No | N/A | No new processes or GenServers needed. |
| IV. Test-First Development | Yes | PASS | Tests planned for: LiveView (wizard flow, empty states), JS lib (onboarding.js), JS hook (onboarding_hook.js). |
| V. Contracts and Behaviours | No | N/A | No new behaviours needed — wizard is a UI concern. |
| VI. Static Analysis | Yes | PASS | All public functions will have @spec. ESLint + Prettier for JS. |
| VII. Lean LiveViews | Yes | PASS | ConnectLive delegates to existing domain contexts. Wizard is presentation-only. JS hook is minimal (localStorage wiring). |
| VIII. retro Design Fidelity | Yes | PASS | Wizard uses retro dialog patterns. Empty states use design tokens (--color-muted, --text-sm). |
| IX. Hot/Cold Data Separation | No | N/A | No persistent data. localStorage is browser-side. |
| X. Scalable Architecture | No | N/A | Feature is purely presentational. |
| XI. User-Facing Documentation | Yes | PASS | Help topics planned for wizard and empty states. |

**Gate Result**: PASS — No violations. Proceeding to design.

## Project Structure

### Documentation (this feature)

```text
specs/028-onboarding-empty-states/
├── plan.md              # This file
├── spec.md              # Feature specification
├── research.md          # Phase 0: research decisions
├── data-model.md        # Phase 1: data model (assigns, localStorage)
├── quickstart.md        # Phase 1: quickstart guide
├── contracts/           # Phase 1: event + component contracts
│   ├── liveview-events.md
│   └── components.md
├── checklists/
│   └── requirements.md  # Spec quality checklist
└── tasks.md             # Phase 2 output (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
apps/retro_hex_chat_web/
├── lib/retro_hex_chat_web/
│   ├── live/
│   │   ├── connect_live.ex              # MODIFIED: wizard assigns, hook attachment
│   │   ├── connect_live.html.heex       # MODIFIED: conditional wizard/simple form
│   │   ├── connect_live/
│   │   │   └── wizard_events.ex         # NEW: wizard event handler (attach_hook)
│   │   ├── chat_live.ex                 # MODIFIED: onboarded param, tip banner assign
│   │   └── chat_live.html.heex          # MODIFIED: tip banner, empty channel state
│   └── components/
│       ├── wizard_dialog.ex             # NEW: 3-step wizard dialog component
│       ├── treebar.ex                   # MODIFIED: empty state block
│       ├── nicklist.ex                  # MODIFIED: empty state block
│       └── url_catcher_window.ex        # MODIFIED: empty state block
├── assets/
│   ├── js/
│   │   ├── app.js                       # MODIFIED: register OnboardingHook
│   │   ├── hooks/
│   │   │   └── onboarding_hook.js       # NEW: localStorage check + event wiring
│   │   └── lib/
│   │       └── onboarding.js            # NEW: pure logic (read/write localStorage)
│   ├── css/
│   │   ├── app.css                      # MODIFIED: import new CSS files
│   │   ├── empty-state.css              # NEW: shared empty state styles
│   │   └── wizard-dialog.css            # NEW: wizard dialog styles
│   └── test/
│       ├── hooks/
│       │   └── onboarding_hook.test.js  # NEW: hook behavioral tests
│       └── lib/
│           └── onboarding.test.js       # NEW: lib unit tests
└── test/
    └── retro_hex_chat_web/
        └── live/
            ├── connect_live_test.exs    # MODIFIED: wizard flow tests
            └── chat_live_test.exs       # MODIFIED: tip banner, empty state tests

apps/retro_hex_chat/
└── lib/retro_hex_chat/
    └── chat/
        └── help_topics/
            └── (existing files)         # MODIFIED: add onboarding + empty state topics
```

**Structure Decision**: Existing umbrella structure. All changes in retro_hex_chat_web except help topics in retro_hex_chat domain. The wizard event handler follows the established `live/chat_live/` subdirectory pattern, placed in `live/connect_live/` for consistency. JS follows hook=wiring/lib=logic pattern.

## Complexity Tracking

> No constitution violations — this section is empty.

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| — | — | — |
