# Implementation Plan: Scripting & Aliases (Simplified)

**Branch**: `018-scripting-aliases` | **Date**: 2026-02-12 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/018-scripting-aliases/spec.md`

## Summary

Implement a simplified scripting system inspired by mIRC: user-defined command aliases with variable expansion ($1–$9, $nick, $chan), session-only timers (one-shot and repeating via Process.send_after), custom popup menu items for nicklist and channel context menus, and event-triggered auto-respond rules with rate limiting. Three new PostgreSQL tables persist aliases, custom menus, and auto-respond rules for registered users. All subsystems share a centralized variable expansion engine. Four new slash commands (/alias, /timer, /popups, /autorespond), three 98.css-styled editor dialogs, and comprehensive help documentation.

## Technical Context

**Language/Version**: Elixir 1.17+ / OTP 27+
**Primary Dependencies**: Phoenix 1.8+, Phoenix LiveView 1.0+, Ecto 3.x, 98.css
**Storage**: PostgreSQL 16+ (3 new tables: `aliases`, `custom_menu_items`, `autorespond_rules`) + in-memory Session state for guests + socket assigns for timers and rate limit cooldowns
**Testing**: ExUnit with async: true, Mox, ExMachina, StreamData, Floki
**Target Platform**: Web (Phoenix LiveView)
**Project Type**: Umbrella (retro_hex_chat + retro_hex_chat_web)
**Performance Goals**: Alias expansion < 1ms, timer fire within 2s of scheduled time
**Constraints**: Max 5 concurrent timers per user, max 10 auto-respond rules, max 10 custom menu items per type, alias expansion depth max 5
**Scale/Scope**: Per-user feature state, no cross-user interactions beyond auto-respond event triggers

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Elixir & Phoenix Exclusive | PASS | All implementation in Elixir/Phoenix/LiveView. No JS frameworks. |
| II. Umbrella with Bounded Contexts | PASS | Domain modules in `retro_hex_chat` (Chat context). Web components in `retro_hex_chat_web`. |
| III. OTP Process Architecture | PASS | No new OTP processes needed. Timers use Process.send_after in existing LiveView process. This is appropriate — max 5 timers per user doesn't justify a dedicated GenServer. |
| IV. Test-First Development | PASS | Unit tests for all domain modules (AliasList, AliasExpander, CustomMenus, AutoRespondRules, TimerManager). Integration tests for persistence. LiveView tests for dialogs. |
| V. Contracts and Behaviours | PASS | 4 new command handlers implement Handler behaviour. All public functions have @spec. |
| VI. Static Analysis from Day One | PASS | All new modules will have @spec, pass Credo strict, Dialyzer, and format checks. |
| VII. Lean LiveViews | PASS | All business logic in domain modules. LiveView delegates to Chat context. Dialog components are function components. |
| VIII. Windows 98 Design Fidelity | PASS | 3 new editor dialogs use 98.css classes (window, title-bar, tree-view, tablist). |
| IX. Hot/Cold Data Separation | PASS | Timers + rate limit cooldowns in memory (hot). Aliases, custom menus, auto-respond rules in PostgreSQL (cold). Session state bridges both. |
| X. Scalable Architecture | PASS | No new processes or global state. Per-user Session state scales with LiveView connections. |
| XI. User-Facing Documentation | PASS | Help topics for /alias, /timer, /popups, /autorespond commands + Aliases, Timers, Custom Menus, Auto-Respond feature topics. |

**Post-design re-check**: All principles still pass. No violations to justify.

## Project Structure

### Documentation (this feature)

```text
specs/018-scripting-aliases/
├── plan.md              # This file
├── spec.md              # Feature specification
├── research.md          # Phase 0: research decisions
├── data-model.md        # Phase 1: entity definitions
├── quickstart.md        # Phase 1: developer onboarding
├── contracts/           # Phase 1: module contracts
│   └── domain-modules.md
├── checklists/
│   └── requirements.md  # Spec quality checklist
└── tasks.md             # Phase 2 output (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
apps/retro_hex_chat/
├── lib/retro_hex_chat/
│   ├── accounts/
│   │   └── session.ex                    # Modified: 3 new fields
│   ├── chat/
│   │   ├── alias_expander.ex             # NEW: Variable expansion engine
│   │   ├── alias_list.ex                 # NEW: Alias CRUD + persistence
│   │   ├── alias_entry.ex                # NEW: Alias value object
│   │   ├── custom_menus.ex               # NEW: Custom menu CRUD + persistence
│   │   ├── custom_menu_item.ex           # NEW: Menu item value object
│   │   ├── auto_respond_rules.ex         # NEW: Auto-respond CRUD + persistence
│   │   ├── auto_respond_rule.ex          # NEW: Rule value object
│   │   ├── timer_manager.ex              # NEW: Timer validation/formatting
│   │   ├── help_topics.ex                # Modified: new help topics
│   │   └── schemas/
│   │       ├── alias_entry.ex            # NEW: Ecto schema
│   │       ├── custom_menu_item.ex       # NEW: Ecto schema
│   │       └── auto_respond_rule.ex      # NEW: Ecto schema
│   └── commands/
│       ├── registry.ex                   # Modified: 4 new entries
│       └── handlers/
│           ├── alias.ex                  # NEW: /alias handler
│           ├── timer.ex                  # NEW: /timer handler
│           ├── popups.ex                 # NEW: /popups handler
│           └── auto_respond.ex           # NEW: /autorespond handler
├── priv/repo/migrations/
│   ├── *_create_aliases.exs              # NEW
│   ├── *_create_custom_menu_items.exs    # NEW
│   └── *_create_autorespond_rules.exs    # NEW
└── test/
    └── retro_hex_chat/
        ├── chat/
        │   ├── alias_expander_test.exs   # NEW
        │   ├── alias_list_test.exs       # NEW
        │   ├── custom_menus_test.exs     # NEW
        │   ├── auto_respond_rules_test.exs # NEW
        │   └── timer_manager_test.exs    # NEW
        └── commands/handlers/
            ├── alias_test.exs            # NEW
            ├── timer_test.exs            # NEW
            ├── popups_test.exs           # NEW
            └── auto_respond_test.exs     # NEW

apps/retro_hex_chat_web/
├── lib/retro_hex_chat_web/
│   ├── live/
│   │   └── chat_live.ex                  # Modified: alias dispatch, timer handle_info,
│   │                                     #   dialog events, auto-respond hooks, persistence
│   └── components/
│       ├── alias_dialog.ex               # NEW: Alias Editor dialog
│       ├── custom_menus_dialog.ex        # NEW: Custom Menus editor dialog
│       ├── auto_respond_dialog.ex        # NEW: Auto-Respond editor dialog
│       ├── menu_bar.ex                   # Modified: 3 new Tools menu items
│       ├── context_menu.ex               # Modified: custom items after separator
│       └── treebar_context_menu.ex       # Modified: custom items after separator
└── test/
    └── retro_hex_chat_web/live/
        └── chat_live_alias_test.exs      # NEW: LiveView integration tests
```

**Structure Decision**: Follows existing umbrella structure. All new domain modules go in `RetroHexChat.Chat` context (the natural home for user-facing chat features). Command handlers follow the established one-module-per-command pattern under `Commands.Handlers`. Web components follow the existing function component pattern.

## Complexity Tracking

> No constitution violations. Table left empty.

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| — | — | — |
