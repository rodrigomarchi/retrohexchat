# Implementation Plan: Miscellaneous Polish

**Branch**: `022-misc-polish` | **Date**: 2026-02-13 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/022-misc-polish/spec.md`

## Summary

Implement 12 polish features that collectively improve daily usability: nick column alignment (CSS grid), double-click actions (nicklist→PM, channel→join, URL→open), right-click copy (JS clipboard API), character counter (JS hook with hard cap at 1000), multi-line paste dialog (JS paste detection + LiveView retro dialog + 300ms paced send with 100-line hard cap), quit message broadcast (PubSub to all channels), away auto-reply (MapSet tracking, once per sender), timestamp format configuration (extend UserPreferences + Options Dialog), emoji picker (static dataset + component + toolbar button), about dialog enhancement (ASCII art + credits), and help menu quick access (open help at specific topics). No new database migrations — extends existing `user_preferences` JSONB column.

## Technical Context

**Language/Version**: Elixir 1.17+ / OTP 27+
**Primary Dependencies**: Phoenix 1.8+, Phoenix LiveView 1.0+, Ecto 3.x, retro design system
**Storage**: PostgreSQL 16+ (existing `user_preferences` table, `display_settings` JSONB column extended with `timestamp_format` and `quit_message` keys) + in-memory socket assigns for runtime state
**Testing**: ExUnit (unit, integration, liveview, e2e tags), Floki for HTML assertions
**Target Platform**: Web (modern browsers)
**Project Type**: Umbrella (retro_hex_chat domain + retro_hex_chat_web)
**Performance Goals**: Character counter updates <16ms (client-side JS). Paste pacing: 300ms between messages. Quit broadcast: <1s to all channels.
**Constraints**: No new migrations. JS hooks must be minimal and isolated (Constitution VII). All features work for guest and registered users (registered users persist preferences).
**Scale/Scope**: 12 features across 7 phases. ~1 new domain module (EmojiData), ~3 new components (PasteConfirmDialog, EmojiPicker, AboutDialog), 5 new JS hooks (CharCounterHook, PasteHook, ChatCopyHook, EmojiPickerHook, NicklistHook), extensions to ~10 existing files.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Relevant? | Status | Notes |
|-----------|-----------|--------|-------|
| I. Elixir & Phoenix Exclusive | Yes | PASS | All server logic in Elixir/Phoenix. JS hooks are minimal (clipboard, paste detection, character counting, emoji insertion) per Constitution VII allowance. |
| II. Umbrella with Bounded Contexts | Yes | PASS | EmojiData goes in `retro_hex_chat/chat/`. All web components in `retro_hex_chat_web/components/`. No cross-boundary violations. |
| III. OTP Process Architecture | No | N/A | No new GenServers or processes. Uses existing PubSub and channel server infrastructure. |
| IV. Test-First Development | Yes | PASS | Each phase includes unit, integration, liveview, and e2e tests. TDD workflow enforced. |
| V. Contracts and Behaviours | Yes | PASS | EmojiData has `@spec` on all functions. No new behaviours needed (features don't add commands requiring Handler). |
| VI. Static Analysis from Day One | Yes | PASS | All new code must pass `mix format`, `mix credo --strict`, `mix dialyzer`. `@spec` on all public functions. |
| VII. Lean LiveViews & Components | Yes | PASS | JS hooks limited to: clipboard, paste detection, character counting, emoji cursor insertion. LiveViews delegate to contexts. |
| VIII. retro Design Fidelity | Yes | PASS | Paste dialog, emoji picker, about dialog all use retro styling. Nick column uses monospace font. Context menu matches existing retro design system pattern. Dark theme counterparts for all new CSS. |
| IX. Hot/Cold Data Separation | Yes | PASS | Runtime state (away_replied_to, paste_lines, quit_reason) in socket assigns. Preferences in JSONB (existing). No new hot/cold boundaries. |
| X. Scalable Architecture | No | N/A | No architectural changes. All features are per-connection. |
| XI. User-Facing Documentation | Yes | PASS | Help topics for: nick alignment, copy, paste dialog, character counter, quit messages, away auto-reply, emoji picker, timestamp format. Keyboard shortcuts topic updated. |

**Gate result: ALL PASS** — no violations, no complexity tracking needed.

## Project Structure

### Documentation (this feature)

```text
specs/022-misc-polish/
├── plan.md              # This file
├── spec.md              # Feature specification
├── research.md          # Phase 0: research decisions
├── data-model.md        # Phase 1: entity definitions
├── quickstart.md        # Phase 1: implementation guide
├── contracts/           # Phase 1: module contracts
│   └── module-contracts.md
├── checklists/
│   └── requirements.md  # Spec quality checklist
└── tasks.md             # Phase 2 output (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
apps/retro_hex_chat/
├── lib/retro_hex_chat/
│   └── chat/
│       ├── emoji_data.ex              # NEW: Static emoji dataset
│       ├── user_preferences.ex        # MODIFY: Add timestamp_format, quit_message
│       ├── display_preferences.ex     # MODIFY: Add :none timestamp format
│       └── help_topics/
│           └── features.ex            # MODIFY: Add 8+ new help topics
└── test/retro_hex_chat/
    └── chat/
        ├── emoji_data_test.exs        # NEW: Unit tests
        └── user_preferences_test.exs  # MODIFY: New field tests

apps/retro_hex_chat_web/
├── assets/
│   ├── css/
│   │   ├── chat.css                   # MODIFY: Nick column grid, counter, paste dialog, emoji picker
│   │   └── dark-theme.css             # MODIFY: Dark theme counterparts
│   └── js/hooks/
│       ├── index.js                   # MODIFY: Register new hooks
│       ├── char_counter_hook.js       # NEW: Character counter
│       ├── paste_hook.js              # NEW: Multi-line paste detection
│       ├── chat_copy_hook.js          # NEW: Right-click copy
│       ├── emoji_picker_hook.js       # NEW: Emoji cursor insertion
│       └── nicklist_hook.js           # NEW: Nicklist double-click detection
├── lib/retro_hex_chat_web/
│   ├── components/
│   │   ├── paste_confirm_dialog.ex    # NEW: Multi-line paste confirmation
│   │   ├── emoji_picker.ex           # NEW: Emoji picker popup
│   │   ├── about_dialog.ex           # NEW: Enhanced about dialog
│   │   ├── menu_bar.ex               # MODIFY: Add Help menu items
│   │   ├── nicklist.ex               # (unchanged — dblclick handled by NicklistHook JS + context_menu_events.ex)
│   │   ├── options_dialog.ex         # MODIFY: Timestamp format + quit message settings
│   │   └── formatting_toolbar.ex     # MODIFY: Add emoji button
│   └── live/
│       ├── chat_live.ex               # MODIFY: new format_time/2, assigns
│       ├── chat_live.html.heex        # MODIFY: Nick column layout, counter, picker, dialogs
│       └── chat_live/
│           ├── core_events.ex         # MODIFY: Paste events
│           ├── context_menu_events.ex # MODIFY: Nicklist dblclick → PM
│           ├── menu_toolbar_events.ex # MODIFY: Quit broadcast
│           ├── options_events.ex      # MODIFY: Timestamp + quit message handlers
│           ├── help_events.ex         # MODIFY: open_help_at_topic
│           ├── helpers/
│           │   ├── channel.ex         # MODIFY: Quit broadcast in cleanup
│           │   └── session.ex         # MODIFY: Away auto-reply (handle_set_away toggle), quit reason
│           └── pubsub_handlers/
│               └── messages.ex        # MODIFY: Away auto-reply on PM receive
└── test/retro_hex_chat_web/
    └── live/
        ├── chat_live_polish_test.exs  # NEW: Integration tests for all features
        └── e2e/
            └── polish_e2e_test.exs    # NEW: E2E tests
```

**Structure Decision**: Existing umbrella structure. New domain module `Chat.EmojiData` in the `chat` bounded context. New web components follow established `components/` pattern. New JS hooks follow established `hooks/` pattern. No structural changes to the project.

## Complexity Tracking

> No constitution violations. Table intentionally left empty.

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| *(none)* | — | — |
