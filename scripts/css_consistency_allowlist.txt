# CSS Consistency Allowlist
# Three sections: [unused], [missing], and [dynamic-prefixes].
# Keep this file narrow: every entry must map to a current runtime, vendor, or
# parser false-positive case. The linter fails stale entries and broad globs.

[unused]
# False positive: the selector scanner sees ".png" inside avatar image URLs.
png

# IRC formatter classes are generated from numeric color codes at runtime.
irc-fg-*
irc-bg-*

# highlight.js injects these token classes into rendered code blocks at runtime.
hljs*

# Markdown renderer adds this to sanitized inline images at runtime.
chat-markdown-image

# P2P diagram state/browser modifiers are composed from connection state data.
p2p-diagram-strip--*
p2p-diagram__badge--*
p2p-diagram__browser-svg--*
p2p-diagram__link--*

# Virtual-space character sprites are composed from the selected character id.
rh-charsel-sprite--*

[missing]
# JS-created/queried chat hooks with no standalone visual CSS.
chat-action
chat-content
chat-timestamp

# Chat search/autocomplete hooks toggle these transient DOM states.
hist-search-panel--open
history-no-match
history-no-match--visible
history-search-input
search-highlight
search-highlight-active

# Runtime overlays and JS-created UI.
dialog-overlay
game-canvas__stub
toast-actions
toast-checkbox
toast-container
toast-hiding
toast-notification
toast-text
toast-visible

# Legacy-free selector contracts: used by JS/runtime DOM, not by CSS.
file-transfer-input
selected
title-bar
title-bar-text
window
window-body

[dynamic-prefixes]
# Message row type/status modifiers come from message domain state.
chat-message--

# Resizer direction handles are generated from a fixed direction list.
desktop-window__resize--

# Nick colors are generated from nickname hash or custom color selection.
nick-color-
