# CSS Consistency Allowlist
# Three sections: [unused], [missing], [dynamic-prefixes]
# Lines starting with # are comments, blank lines are ignored.
# Entries ending with * are glob patterns (prefix match).
# Dynamic prefixes suppress matches in both directions.

[unused]
# IRC format classes — generated dynamically by formatter.ex (irc-fg-0..15, irc-bg-0..15)
irc-fg-*
irc-bg-*
# Utility classes — predefined library, may not all be used yet
u-*
# Password masking — applied via class="input-masked" in connect_live.ex
input-masked
# JS-hook only — classes added dynamically via classList in JS hooks
attempt-info
char-counter--warning
char-counter--danger
connection-banner--disconnected
connection-banner--reconnected
# Connection status — JS hook toggles these classes dynamically (ConnectionStatusHook)
status-bar-connection--connecting
status-bar-connection--reconnecting
# Conversations user list — nick-away applied dynamically via user_item_class/1 in conversations.ex
nick-away
# Media call device panel — rendered dynamically by JS after enumerateDevices
media-call__device-fallback
media-call__device-label
media-call__devices
# Legacy utility — kept for backwards compat but currently unused
licon-32
# highlight.js theme — classes applied by highlight.js library at runtime (not in our templates)
hljs*
# retrohex.css components — used by V2 templates in Tailwind paths (skipped by template scanner)
retro-fieldset
toolbar-group
tree-view-marker-closed
tree-view-marker-open

[missing]
# False positive — "system" is a comparison value inside class={...} expression
system
# JS-only — toast body queried via querySelector in notification_toast.js
toast-body
# Semantic modifier classes — used in dynamic class expressions, no distinct styling needed
p2p-lobby-chat__content
p2p-lobby-consent__btn--reject
p2p-lobby-presence__user--offline
p2p-lobby-presence__user--online
# Semantic container classes with no visual styling needed
cheatsheet-dialog
context-color-picker
# Media call — semantic container, no visual styling needed
media-call__video-area
# False positive — "video" parsed from dynamic class expression "media-call--video"
video
# False positives — English text parsed as class refs from p2p_lobby.ex connection_state_class
Connected
Connection
failed
# False positive — "finished" is a comparison value in solo_lobby.ex class={...} expression
finished
# Menu bar — Tailwind classes toggled via classList in MenuBarHook
bg-primary
text-primary-foreground
hover:bg-accent
# Help layout — uses old app_header class from controller-era layout
app-header-panels
# Chat layout — Tailwind utility classes from tailwind.config.js (not in CSS files)
bg-desktop
font-system
text-text
# JS hooks — classes used by JS hooks for DOM manipulation and querySelector targeting.
# These were previously defined in V1 retro CSS; now styled via Tailwind or used as selectors only.
chat-action
chat-content
chat-link
chat-link-preview
chat-nick
chat-reply-btn
chat-timestamp
connection-banner--visible
reconnect-overlay--visible
conversations-join-flash
dialog-overlay
disabled
file-transfer-drop-zone--active
file-transfer-input
focused
format-color-dropdown
format-color-dropdown--open
game-canvas__stub
hist-search-panel--open
history-no-match
history-no-match--visible
history-search-input
interactive-tooltip
new-messages-btn
new-messages-btn--visible
p2p-diagram__dot
p2p-diagram__dots
search-highlight
search-highlight-active
selected
title-bar
title-bar-text
toast-actions
toast-checkbox
toast-container
toast-hiding
toast-notification
toast-text
toast-visible
window
window-body

[dynamic-prefixes]
# Prefixes with dynamic suffixes (e.g. chat-message--#{type})
chat-message--
# Nick color classes — generated dynamically by nick_color_fn (nick-color-0..11)
nick-color-
