# One finding ID per line.
#
# Use this only for literals that are intentionally not translated, such as
# protocol tokens, IRC commands, CSS/DOM ids, event names, test ids, log-only
# messages, or proper names that must remain unchanged.
#
# Example:
# abc123def456 # apps/example.ex:42 - protocol command, not UI

