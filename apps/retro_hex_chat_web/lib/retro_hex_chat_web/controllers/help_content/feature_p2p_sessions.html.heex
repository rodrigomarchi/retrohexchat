<p>
  P2P sessions allow direct communication between two registered users for chat, file transfer, and audio/video calls.
</p>
<.help_h4 icon={:icon_p2p}>Creating a Session</.help_h4>
<p><strong>Via commands:</strong></p>
<pre>/p2p &lt;nickname&gt;</pre>
<p>Starts a generic session. The target user receives an invite and must accept.</p>
<pre>/call &lt;nickname&gt;</pre>
<p>Starts an audio call session. Automatically requests permission after entering the lobby.</p>
<pre>/sendfile &lt;nickname&gt;</pre>
<p>Starts a file transfer session.</p>
<p>
  <strong>Via context menu:</strong>
  Click a nick (in the user list or chat) and select P2P Session, Audio Call, Video Call, or Send File. P2P items only appear for registered users and are disabled if the target is not registered.
</p>
<.help_h4 icon={:icon_p2p}>Lobby</.help_h4>
<p>
  The lobby is the session waiting room. Both users can chat and request actions (calls, transfers). All actions require
  <strong>bilateral consent</strong>
  — the other user must accept before the action starts.
</p>
<.help_h4 icon={:icon_p2p}>Session Types</.help_h4>
<ul>
  <li><strong>Generic</strong> — chat with action options</li>
  <li><strong>File Transfer</strong> — auto-requests file transfer</li>
  <li><strong>Audio Call</strong> — auto-requests audio call</li>
  <li><strong>Video Call</strong> — auto-requests video call</li>
</ul>
<.help_h4 icon={:icon_clock}>Timeouts</.help_h4>
<p>
  Pending sessions (waiting for the peer) expire in 5 minutes. The lobby expires due to inactivity after 15 minutes. The WebRTC connection has a 30-second timeout.
</p>
<.help_h4 icon={:icon_phone_end}>Ending</.help_h4>
<p>Click <strong>End Session</strong> or close the tab. The other user will be notified.</p>
<.help_h4 icon={:icon_link}>See Also</.help_h4>
<p>
  <.help_link topic="feature-file-transfer">File Transfer</.help_link>
  · <.help_link topic="feature-audio-call">Audio Call</.help_link>
  · <.help_link topic="feature-video-call">Video Call</.help_link>
  · <.help_link topic="feature-p2p-games">P2P Games</.help_link>
  · <.help_link topic="feature-privacy-settings">Privacy Settings</.help_link>
  · <.help_link topic="feature-context-menus">Context Menus</.help_link>
</p>
