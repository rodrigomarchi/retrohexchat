<p>{gettext("Sets a password (key) that users must provide to join the channel.")}</p>
<.help_h4 icon={:icon_terminal}>{gettext("Usage")}</.help_h4>
<pre>{gettext("/mode +k secretpass — Set channel key /mode -k — Remove channel key /join #channel secretpass — Join with key")}</pre>
