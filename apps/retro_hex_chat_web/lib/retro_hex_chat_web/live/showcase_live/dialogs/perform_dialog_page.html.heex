<.showcase_layout active_page={@active_page}>
  <h2 class="text-lg font-bold mb-3">{dgettext("showcase", "Perform Dialog")}</h2>

  <.showcase_card
    title={dgettext("showcase", "Perform Dialog")}
    description="Win98-style dialog for managing the commands executed automatically on connect."
  >
    <.button phx-click={show_modal("perform-demo")}>
      <:icon><Icons.icon_btn_perform /></:icon>
      {dgettext("showcase", "Open Perform")}
    </.button>

    <.perform_dialog
      id="perform-demo"
      show={false}
      entries={@sample_commands}
      selected={@selected}
      enabled={@enabled}
      on_select="select-perform"
      on_toggle_enabled="toggle-perform-enabled"
    />

    <.code_example>
      &lt;.perform_dialog
      id="perform"
      show=&#123;true&#125;
      entries=&#123;@commands&#125;
      selected=&#123;@selected&#125;
      enabled=&#123;@enabled&#125;
      on_select="select"
      on_add="add"
      on_edit="edit"
      on_remove="remove"
      on_move_up="move-up"
      on_move_down="move-down"
      on_toggle_enabled="toggle-enabled"
      on_ok="save"
      on_cancel="cancel"
      /&gt;
    </.code_example>
  </.showcase_card>

  <.showcase_card
    title={dgettext("showcase", "Password Masking")}
    description="NickServ IDENTIFY passwords are automatically masked in the commands list for security."
  >
    <div class="shadow-retro-field bg-white p-2">
      <p class="text-xs mb-1">
        <span class="font-bold">{dgettext("showcase", "Input:")}</span>
        <code class="font-mono">
          {dgettext("showcase", "/msg NickServ IDENTIFY mySecretPass")}
        </code>
      </p>
      <p class="text-xs">
        <span class="font-bold">{dgettext("showcase", "Display:")}</span>
        <code class="font-mono">{dgettext("showcase", "/msg NickServ IDENTIFY ***")}</code>
      </p>
    </div>
  </.showcase_card>

  <.showcase_card
    title={dgettext("showcase", "Component Attributes")}
    description="Full list of supported attributes for the perform_dialog component."
  >
    <div class="overflow-x-auto">
      <table class="w-full text-xs">
        <thead>
          <tr class="border-b">
            <th class="text-left px-2 py-1">{dgettext("showcase", "Attribute")}</th>
            <th class="text-left px-2 py-1">{dgettext("showcase", "Type")}</th>
            <th class="text-left px-2 py-1">{dgettext("showcase", "Default")}</th>
            <th class="text-left px-2 py-1">{dgettext("showcase", "Description")}</th>
          </tr>
        </thead>
        <tbody>
          <tr class="border-b">
            <td class="px-2 py-1 font-mono">id</td>
            <td class="px-2 py-1">string</td>
            <td class="px-2 py-1">required</td>
            <td class="px-2 py-1">{dgettext("showcase", "Dialog element ID")}</td>
          </tr>
          <tr class="border-b">
            <td class="px-2 py-1 font-mono">entries</td>
            <td class="px-2 py-1">list</td>
            <td class="px-2 py-1">[]</td>
            <td class="px-2 py-1">
              {dgettext("showcase", "List of %&#123;position, command&#125; maps")}
            </td>
          </tr>
          <tr class="border-b">
            <td class="px-2 py-1 font-mono">enabled</td>
            <td class="px-2 py-1">boolean</td>
            <td class="px-2 py-1">true</td>
            <td class="px-2 py-1">{dgettext("showcase", "Enable perform on connect")}</td>
          </tr>
          <tr>
            <td class="px-2 py-1 font-mono">{dgettext("showcase", "on_*")}</td>
            <td class="px-2 py-1">any</td>
            <td class="px-2 py-1">nil</td>
            <td class="px-2 py-1">
              {dgettext("showcase", "Event callbacks (select, add, edit, remove, etc.)")}
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  </.showcase_card>
</.showcase_layout>
